# EditorConfig is awesome: http://EditorConfig.org

# top-most EditorConfig file
root = true

# Newline ending every file
[*]
insert_final_newline = true

#### Only one of these seems to work at a time .... 
# Trim whitespace
[*]
trim_trailing_whitespace = true
